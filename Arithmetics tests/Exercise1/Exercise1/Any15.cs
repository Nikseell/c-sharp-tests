using System;

namespace Exercise1
{
    public class Any15
    {
        public bool Has15(int x, int y)
        {
            return (x == 15 || y == 15 || x + y == 15 || x - y == 15 || x - y == 15);
        }
    }
}
